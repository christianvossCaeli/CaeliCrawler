# CaeliCrawler Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# ============================================
# APPLICATION
# ============================================
APP_ENV=development
DEBUG=false

# ============================================
# DATABASE
# ============================================
# PostgreSQL password (REQUIRED for production)
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=your_secure_password_here

# ============================================
# AI SERVICES
# ============================================
# AI service credentials (Azure OpenAI, Anthropic, SerpAPI, etc.)
# are now configured via the Admin UI: Admin > API Credentials
#
# This provides:
# - Secure storage in database (encrypted)
# - Per-user credential management
# - Runtime configuration without restarts
# - Audit logging of credential changes
#
# See docs/api/ADMIN.md for API Credentials endpoints.

# ============================================
# EXTERNAL INTEGRATIONS - PySis
# ============================================
PYSIS_API_BASE_URL=https://your-pysis-instance.com/api/pisys/v1
PYSIS_TENANT_ID=your_tenant_id
PYSIS_CLIENT_ID=your_client_id
PYSIS_CLIENT_SECRET=your_pysis_client_secret
PYSIS_SCOPE=api://your-app-id/.default

# ============================================
# EXTERNAL INTEGRATIONS - Caeli Auction
# ============================================
CAELI_AUCTION_MARKETPLACE_API_AUTH=your_caeli_auction_auth

# ============================================
# MONITORING (Optional)
# ============================================
# Grafana admin password (generate with: openssl rand -base64 24)
GRAFANA_PASSWORD=your_secure_grafana_password

# ============================================
# SECURITY NOTES
# ============================================
# 1. Use strong, unique passwords for all services
# 2. In production, use secret management (AWS Secrets Manager, HashiCorp Vault)
# 3. Never log or expose these values in error messages
# 4. Never commit .env files to version control
# 5. Set DEBUG=false in production
